{
  "name": "ensure-source-branch-contains-destination-branch",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "main": "index.js",
  "scripts": {
    "build": "ncc build index.js --license licenses.txt -o dist",
    "lint": "npm run lint:prettier && npm run lint:eslint",
    "lint:eslint": "eslint .",
    "lint:prettier": "prettier --check .",
    "format": "prettier --write .",
    "test": "npm run test:unit && npm run test:integration",
    "test:unit": "vitest run",
    "test:integration": "act # Does not work since it is not clear how to forward SSH",
    "postinstall": "[ \"$CI\" = \"true\" ] || npx simple-git-hooks"
  },
  "dependencies": {
    "@actions/core": "^1.11.1",
    "@actions/github": "^9.1.0"
  },
  "devDependencies": {
    "@vercel/ncc": "^0.38.3",
    "eslint": "^8.57.1",
    "eslint-plugin-vitest": "^0.5.4",
    "prettier": "^3.8.2",
    "simple-git-hooks": "^2.13.1",
    "vitest": "^4.1.4"
  },
  "simple-git-hooks": {
    "pre-commit": "npm run format && npm run lint && npm run test:unit && npm run build && git add -u"
  }
}
